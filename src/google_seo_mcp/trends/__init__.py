"""Google Trends + Google Suggest + Google Alerts RSS.

All free, no auth needed. Trends via pytrends (unofficial — Google rate-limits
to ~10 QPM per IP), Suggest via the public completion endpoint, Alerts via the
public RSS feed URL the user creates manually at google.com/alerts.
"""
